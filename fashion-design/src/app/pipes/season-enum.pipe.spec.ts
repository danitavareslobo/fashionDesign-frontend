import { SeasonEnumPipe } from './season-enum.pipe';

describe('SeasonEnumPipe', () => {
  it('create an instance', () => {
    const pipe = new SeasonEnumPipe();
    expect(pipe).toBeTruthy();
  });
});
